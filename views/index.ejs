<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MQTT and Express</title>
</head>
<body>
  <h1>MQTT and Express Demo</h1>
  <p>Message that will get: <strong id="message"><%= message %></strong></p>

  <script>
    setInterval(() => {
      fetch('/')
        .then(response => response.text())
        .then(html => {
          document.querySelector('#message').innerHTML = 
            new DOMParser().parseFromString(html, 'text/html').querySelector('#message').innerHTML;
        });
    }, 1000); // Poll the server every second for updates
  </script>
</body>
</html>
